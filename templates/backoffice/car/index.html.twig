{% extends 'base.html.twig' %}

{% block title %}Car index{% endblock %}

{% block body %}
<section class="home-hero mb-4 ">
      <div class="home-hero-inner space-top space-bottom">
        <div class="container">
          <div class="row justify-content-center text-center">
            <div class="col-lg-6">
              <div class="title">
                <h1 class="white-text mb-4 p-8" style="font-size: 40px;">
                  <span class="yellow-text">List of car's Annonunses</span> <br>
                  
                </h1>
                
              </div>
             
            </div>
          </div>
        </div>
      </div>
      <div class="home-hero-bottom d-none d-lg-block"></div>
     
    </section>
    </section> 
               <section class="brands-list space-top space-bottom ">
        <div class="container">
            
                <div class="row">
    <h1>Car index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Model</th>
                <th>ReleaseYear</th>
                <th>Slug</th>
                <th>Type</th>
                <th>Price</th>
                <th>FuelType</th>
                <th>Image</th>
                <th>Seats</th>
                <th>OtherSpec</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for car in cars %}
            <tr>
                <td>{{ car.id }}</td>
                <td>{{ car.model }}</td>
                <td>{{ car.releaseYear ? car.releaseYear|date('Y-m-d') : '' }}</td>
                <td>{{ car.slug }}</td>
                <td>{{ car.type }}</td>
                <td>{{ car.price }}</td>
                <td>{{ car.fuelType }}</td>
                <td>{{ car.image }}</td>
                <td>{{ car.seats }}</td>
                <td>{{ car.otherSpec }}</td>
                <td>
                    <a class = "btn" href="{{ path('backoffice_app_car_show', {'id': car.id}) }}">show</a>
                    <a class = "btn" href="{{ path('backoffice_app_car_edit', {'id': car.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class = "btn"href="{{ path('backoffice_app_car_new') }}">Create new</a>
    </div>
       </div>
       </section>
</main>
{% endblock %}
