{% extends 'base.html.twig' %}

{% block body %}
         <section class="home-hero mb-4 ">
      <div class="home-hero-inner space-top space-bottom">
        <div class="container">
          <div class="row justify-content-center text-center">
            <div class="col-lg-6">
              <div class="title">
                <h1 class="white-text mb-4 p-8" style="font-size: 40px;">
                  <span class="yellow-text">Register your car Details </span> <br>
                  
                </h1>
                
              </div>
             
            </div>
          </div>
        </div>
      </div>
      <div class="home-hero-bottom d-none d-lg-block"></div>
     
    </section>
    </section> 
               <section class="brands-list space-top space-bottom ">
        <div class="container">
            
                <div class="row">
<div class="container">
    <h1>Add a car</h1>

    {{ form_start(form , {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.model) }}
    {{ form_row(form.releaseYear) }}
    {{ form_row(form.brands) }}
    {{ form_row(form.type) }}
    {{ form_row(form.fuelType) }}
    {{ form_row(form.price) }}
    {{ form_row(form.otherSpec) }}
    {{ form_row(form.seats) }}
    {{ form_row(form.imageFile) }}
    <button class="btn btn-primary">Add</button>
{{ form_end(form) }}
   
    
    
   
</div>
 </div>
       </div>
       </section>
</main>
{% endblock %}


{% block title %}
Add a car
{% endblock %}